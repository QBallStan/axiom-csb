<form>
  <div class="form-group">
    <label for="healthBonus">Health Bonus</label>
    <input type="number" name="healthBonus" value="{{healthBonus}}" />
    <p class="hint">Additional boxes added to total health.</p>
  </div>

  <div class="form-group">
    <label for="damageThreshold">Damage Penalty Threshold</label>
    <input type="number" name="damageThreshold" value="{{damageThreshold}}" />
    <p class="hint">Boxes needed before penalties are applied.</p>
  </div>

  <button type="submit">Save</button>
</form>